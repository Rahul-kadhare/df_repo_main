config {
    type: "table",
    schema: "test_dataset"
}

SELECT
  DISTINCT drink_id,
  drink_name,
  drink_type,
  '${dataform.projectConfig.vars.execution_date}' as execution_date,
  current_date() as updated_date
FROM
  ${ref("drinks")}

